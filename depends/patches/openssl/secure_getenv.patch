Patches openssl 1.0.2's usage of secure_getenv from glibc 2.17
to instead use __secure_getenv that is compatible with glibc 2.2.5

diff -dur openssl-1.0.2u-orig/crypto/getenv.c openssl-1.0.2u/crypto/getenv.c
--- openssl-1.0.2u-orig/crypto/getenv.c 2019-12-20 13:02:41.000000000 +0000
+++ openssl-1.0.2u/crypto/getenv.c      2021-09-20 02:07:25.351962048 +0000
@@ -17,9 +17,9 @@
 char *ossl_safe_getenv(const char *name)
 {
 #if defined(__GLIBC__) && defined(__GLIBC_PREREQ)
-# if __GLIBC_PREREQ(2, 17)
+# if __GLIBC_PREREQ(2, 3)
 #  define SECURE_GETENV
-    return secure_getenv(name);
+    return __secure_getenv(name);
 # endif
 #endif

